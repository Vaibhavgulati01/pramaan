# demo/

The hosted demo app (Phase 8), a thin UI over the FastAPI
`/adjudicate` endpoint (`src/pramaan/api/`). Deploying it to HF Spaces is
a documented manual follow-up (needs the user's HF account) — the app
code itself is built and runnable locally as part of Phase 8.
