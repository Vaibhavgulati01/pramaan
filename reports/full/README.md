# reports/full/

The headline. Produced once, on the VM (`make data-full && make train &&
make eval SCALE=full`), from the one frozen test set — unsealed exactly
once, SHA recorded at creation. **The only tier that is ever reported as
a result**, and the only source `README.md`'s Results section reads from
(via `scripts/inject_metrics.py`). Empty until the Phase 9 handoff runs.
