# reports/dev/

Local mechanism-validation output (`pramaan eval --scale dev`), sized by
`src/pramaan/risk/power_analysis.py`. Every mechanism is proven correct
here — full ablations, full shift matrix, negative controls. **Headed
`DEV — not a held-out result` throughout, and never inlined into the
README** — linked from it instead. See `docs/EVALUATION_PROTOCOL.md` for
why: this tier is looked at freely while iterating, which is only safe
because nothing from it is ever claimed as a result.
