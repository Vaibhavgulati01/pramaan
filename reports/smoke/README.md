# reports/smoke/

CI-tier output (`pramaan eval --scale smoke`, ~200 claims, <5 min budget).
Proves the pipeline executes on every push. **Never a reported result** —
see `docs/EVALUATION_PROTOCOL.md`.
