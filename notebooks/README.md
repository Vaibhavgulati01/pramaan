# notebooks/

Outputs only — exploratory notebooks used while building a phase, never
the source of truth for a number or figure. Anything a notebook produces
that belongs in the repo gets turned into a script under `eval/` or
`scripts/` that `make`/`pramaan` can re-run deterministically.
